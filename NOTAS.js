// loadDb, solucion implementada pero sin exito:

// const heightMinMax = api.data.map((el) => el.height.metric.split(" ").length > 1
// ? el.height.metric.split(" ")
// : null
// );

// ------------------- Evaluo si singleheight es null -------------------
// const singleHeight =
//     heightMinMax === null
//     ? api.data.map((el) => el.height.metric.split(""))
//     : null;
// Esto retorna null
